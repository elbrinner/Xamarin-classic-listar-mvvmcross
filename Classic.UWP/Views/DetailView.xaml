<views:MvxWindowsPage
    x:Class="Classic.UWP.Views.DetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Classic.UWP.Views"
    xmlns:views="using:MvvmCross.WindowsUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
     
            <ScrollViewer>
                   <StackPanel>
                 <TextBlock FontSize="35" Text="{Binding SelectedItem.title}"/>
                  <Image HorizontalAlignment="Center" Source="{Binding Image}"></Image>
                <Button Content="Volver"  Command="{Binding BackCommand}"></Button>
              </StackPanel>
              </ScrollViewer>
             
     
     
    </Grid>
</views:MvxWindowsPage>
 
